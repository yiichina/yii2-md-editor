(function ($) {
    'use strict';
    jQuery.extend({
        setUploader: function(obj, type) {
            if(type == 'image') {

            }
            $('#uploader').modal();
        }
    });
})(window.jQuery);